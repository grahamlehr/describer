# Design notes

One note per addendum, numbered in the order they were written. Each
records what was decided, why, what was measured and how it was checked.
Newer notes win over older ones, and `CLAUDE.md` (the current state and the
rules) wins over both. The index, with what each note covers, is at the end
of `CLAUDE.md`.
